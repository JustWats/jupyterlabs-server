# Docker hard caps for every notebook, terminal, and worker combined.
LAB_MEMORY_LIMIT=4g
LAB_CPU_LIMIT=2.0
LAB_PIDS_LIMIT=1024
LAB_BIND=0.0.0.0
LAB_PORT=9999
LAB_SHM_SIZE=2gb
# NVIDIA override exposes just this host device ID/UUID (or a provisioned MIG ID).
LAB_GPU_DEVICE=0
