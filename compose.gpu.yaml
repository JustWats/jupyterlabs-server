services:
  lab:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ["${LAB_GPU_DEVICE:-0}"]
              capabilities: [gpu]
